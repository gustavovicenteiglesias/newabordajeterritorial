import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'abordaje-territorial-app',
  webDir: 'build'
};

export default config;
